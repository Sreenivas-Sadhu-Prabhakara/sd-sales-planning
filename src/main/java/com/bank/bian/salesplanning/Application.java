package com.bank.bian.salesplanning;

import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.annotation.Bean;

/**
 * BIAN Service Domain: Sales Planning
 *
 * Business Area:      Sales and Service
 * Business Domain:    Sales
 * Functional Pattern: Plan
 * Asset Type:         Sales Plan
 * Control Record:     Sales Plan Plan
 *
 * Generated by the bian-platform service generator (Phase 1 — shallow build).
 * Replace the in-memory store with real persistence and domain logic in Phase 2.
 */
@SpringBootApplication
public class Application {

    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }

    @Bean
    public OpenAPI openApi() {
        return new OpenAPI().info(new Info()
                .title("Sales Planning — BIAN Semantic API")
                .description("Service Domain 'Sales Planning' (Plan pattern) "
                        + "operating on the 'Sales Plan Plan' control record.")
                .version("0.1.0"));
    }
}
